
export default interface Macros {
	calories: number;
	protein: number;  // 4 calories per gram
	carbs: number;  // 4 calories per gram
	fat: number;  // 9 calories per gram
	fiber: number;  // 0 calories per gram
}
