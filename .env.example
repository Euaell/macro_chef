# MacroChef Environment Variables
# Copy this file to .env.local and fill in your values

# === Database ===
DB_PASSWORD=mizan_dev_password

# === BetterAuth (Frontend Authentication) ===
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=change_me_in_production_use_openssl_rand

# === BFF Architecture ===
# CRITICAL: This secret must be the same in both frontend and backend
# Generate with: openssl rand -base64 32
BFF_TRUSTED_SECRET=change_me_in_production_use_openssl_rand

# === OpenAI (Optional - for nutrition AI features) ===
OPENAI_API_KEY=sk-your-openai-api-key-here

# === Cloudinary (Image Uploads) ===
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
NEXT_PUBLIC_CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# === Email/SMTP (Password Reset & Notifications) ===
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your_email@example.com
SMTP_PASS=your_email_app_password
SMTP_FROM=MacroChef <noreply@macrochef.com>

# === OAuth Providers (Optional) ===
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# === Production Security Notes ===
# 1. Generate strong random secrets:
#    openssl rand -base64 32
#
# 2. BFF_TRUSTED_SECRET must be identical in frontend and backend
#    This is the security boundary between frontend (public) and backend (internal)
#
# 3. Never commit .env.local to git
#    Add it to .gitignore
#
# 4. In production, use Docker secrets or environment variable management:
#    - AWS Secrets Manager
#    - Kubernetes Secrets
#    - HashiCorp Vault
#    - etc.
